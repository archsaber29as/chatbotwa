app = "chatbotwa"
primary_region = "sin"  # Singapore — closest to Jakarta

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false   # ← keeps it always on
  auto_start_machines = true